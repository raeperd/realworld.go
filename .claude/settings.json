{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "tool-complete": {
      "Write": {
        "command": "cd ${workspaceRoot} && golangci-lint run --fix ${file} && make build && make lint",
        "description": "Run golangci-lint fix, build, and lint after writing Go files",
        "when": "${file} endsWith '.go'"
      },
      "Edit": {
        "command": "cd ${workspaceRoot} && golangci-lint run --fix ${file} && make build && make lint",
        "description": "Run golangci-lint fix, build, and lint after editing Go files",
        "when": "${file} endsWith '.go'"
      }
    }
  }
}
