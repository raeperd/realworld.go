{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "tool-complete": {
      "Write": {
        "command": "cd ${workspaceRoot} && golangci-lint run --fix ${file} && make lint && make build && make test",
        "description": "Run golangci-lint fix, then make lint/build/test after writing Go files",
        "when": "${file} endsWith '.go'"
      },
      "Edit": {
        "command": "cd ${workspaceRoot} && golangci-lint run --fix ${file} && make lint && make build && make test",
        "description": "Run golangci-lint fix, then make lint/build/test after editing Go files",
        "when": "${file} endsWith '.go'"
      }
    }
  }
}
